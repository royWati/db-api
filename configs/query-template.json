[
  {
    "CRUD_TYPE": "SELECT",
    "ORACLE": "SELECT {COLUMN_NAMES} FROM {TABLE_NAME} {WHERE_CLAUSE} {GROUP_STATEMENT} {ORDER_STATEMENT}",
    "MSSQL": "SELECT {TOP_VALUE} {COLUMN_NAMES} FROM {TABLE_NAME} {WHERE_CLAUSE} {GROUP_CLAUSE} {ORDER_STATEMENT}"
  },
  {
    "CRUD_TYPE": "UPDATE",
    "ORACLE": "UPDATE {TABLE_NAME} SET {COLUMN_NAMES} {WHERE_CLAUSE}",
    "MSSQL": "UPDATE {TABLE_NAME} SET {COLUMN_NAMES} {WHERE_CLAUSE}"
  },
  {
    "CRUD_TYPE": "INSERT",
    "ORACLE": "INSERT INTO {TABLE_NAME} ({COLUMN_NAMES}) {VALUES}",
    "MSSQL": "INSERT INTO {TABLE_NAME} ({COLUMN_NAMES}) {VALUES}"
  },
  {
    "CRUD_TYPE": "DELETE",
    "ORACLE": "DELETE FROM {TABLE_NAME}  {WHERE_CLAUSE}",
    "MSSQL": "DELETE FROM {TABLE_NAME}  {WHERE_CLAUSE}"
  },
  {
    "CRUD_TYPE": "SEARCH",
    "ORACLE": "SELECT {COLUMN_NAMES} FROM {TABLE} WHERE {LIKE}",
    "MSSQL": "SELECT {COLUMN_NAMES} FROM {TABLE} WHERE {LIKE}"
  }

]